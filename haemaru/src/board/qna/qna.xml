<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="qna">
    
    <typeAlias alias="qnavo" type="board.qna.QnaVO"/>
    
    <!-- 공통조건 -->
	<sql id="searchSql">		
		<!-- WHERE 절 검색 -->
		<dynamic prepend="where">
			<isNotEqual property="category" compareValue="0">
			    category = #category#
			</isNotEqual>
			<!-- 검색어 검색 -->
			<isNotEqual property="sval" compareValue="">
				<!-- 검색 타입이 all일 때 -->
				<isEqual property="stype" compareValue="all" prepend="and">
					( (name like '%$sval$%' ) or (title like '%$sval$%' )  ) 					
				</isEqual>
				<!-- 검색 타입이 all이 아닐때 해당 stype으로 검색 -->
				<isNotEqual property="stype" compareValue="all" prepend="and">
					($stype$ like '%$sval$%' )
				</isNotEqual>
			</isNotEqual>
		</dynamic>
	</sql>
	
	<!-- 해당 테이블의 총 게시글 수 -->
	<select id="count" parameterClass="qnavo" resultClass="Integer">
        select count(*) from qna
        <include refid="searchSql" />	
    </select>
    
	<select id="list" parameterClass="qnavo" resultClass="qnavo">
        select * from qna
        <include refid="searchSql" />
		ORDER BY registdate DESC LIMIT $startPageNo$, $pageRows$
    </select>
	
	<!-- 해당 테이블에 게시글 작성 -->
    <insert id="insert" parameterClass="qnavo">
        INSERT INTO qna (email, category, title, contents, member_pk, filename, filename_org, registdate, display) VALUES (
        	#email#, #category#, #title#, #contents#, #member_pk#, #filename#, #filename_org#, now(), #display#)
        <selectKey keyProperty="no" resultClass="Integer">
        	SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
    
    <update id="update" parameterClass="qnavo">
        UPDATE qna SET
        	category=#category#, title=#title#, 
       	<isEqual property="filename_chk" compareValue="1">
        	filename='', filename_org=''
        </isEqual>   	
        <isNotEqual property="filename" compareValue="">
            <isNotNull property="filename">
        		filename=#filename#, filename_org=#filename_org#
        	</isNotNull>
        </isNotEqual>
	        contents=#contents#
        WHERE no = #no#
    </update>
     
    <select id="read" parameterClass="qnavo" resultClass="qnavo">
        SELECT 
        	*
        FROM qna
        where no = #no#
    </select>
    
    <select id="filenames" parameterClass="qnavo" resultClass="qnavo">
    	<![CDATA[
        SELECT 
        	file_name
        FROM qna
        where no = #no#
        ]]>
    </select> 
        
    <select id="getRowNum" parameterClass="qnavo" resultClass="Integer">
        select rownum from (
        	select @rownum:=@rownum+1 as rownum, no, title from qna, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY registdate DESC
        ) as r2
        where r2.no = #no#
    </select>
    
    <select id="getNextRowNum" parameterClass="qnavo" resultClass="qnavo">
        select ifnull(rownum,0), ifnull(no,0) as next_no, title as next_title
        from (
        	select @rownum:=@rownum+1 as rownum, no, title from qna, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY registdate DESC
        ) as r2
        <![CDATA[
        where r2.rownum = $rownum$+1
    	]]>
    </select>
    
    <select id="getPrevRowNum" parameterClass="qnavo" resultClass="qnavo">
    	select ifnull(rownum,0), ifnull(no,0) as prev_no, title as prev_title
        from (
        	select @rownum:=@rownum+1 as rownum, no, title from qna, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY registdate DESC
        ) as r2
        <![CDATA[
        where r2.rownum = $rownum$-1
    	]]>
    </select>
    
    <delete id="delete" parameterClass="qnavo">
        DELETE FROM qna WHERE no=#no#
    </delete>
   
    
    <select id="mainList" parameterClass="qnavo" resultClass="qnavo">
        select *
        from qna
        <include refid="searchSql" />
		ORDER BY registdate DESC LIMIT 0, $number$
    </select>
        
      <select id="replylist" parameterClass="Integer" resultClass="board.qna.QnaReplyVO">
    	SELECT title, contents, filename, filename_org, registdate, qna_no FROM qna_reply WHERE qna_no=#no# ORDER BY registdate DESC
      </select>
    
    <insert id = "replyInsert" parameterClass="board.qna.QnaReplyVO">
    	INSERT INTO qna_reply (title, contents, filename, filename_org, registdate, qna_no) values (#title#, #contents#, #filename#, #filename_org#, now(), #qna_no#)
    </insert>
    
    <delete id="replyDelete" parameterClass="Integer">
    	DELETE FROM qna_reply WHERE no=#no#
    </delete>      
 
  
</sqlMap>